plugin.tx_formrelay_salesforce_case{
	# cat=plugin.tx_formrelay_salesforce_case//1; type=int+; label= Enable: Only convert to XML if not set to 0.
	enabled = 1

	# SFDC Sandbox
	salesForceUrl = https://{XYZ}.my.salesforce.com/servlet/servlet.WebToCase

	fields {
		defaults {
			orgid =
			submit = Submit+Query
			retURL = #
			lead_source = Web - TYPO3 formrelay
			encoding = UTF-8

			debugEmail =
			debug = 0
		}
	}
}
