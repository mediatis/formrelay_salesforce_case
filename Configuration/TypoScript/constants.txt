plugin.tx_formrelay_salesforce_case{
	# cat=plugin.tx_formrelay_salesforce_case//1; type=int+; label= Enable: Only convert to XML if not set to 0.
	enabled = 0

	# SFDC Sandbox
	salesForceUrl = https://{XYZ}.my.salesforce.com/servlet/servlet.WebToCase?encoding=UTF-8

	fields {
		defaults {
			orgid =
			retURL = #
			debugEmail =
			debug = 0
		}
	}
}
